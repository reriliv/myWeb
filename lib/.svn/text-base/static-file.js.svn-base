const path = require("path");
const mime = require("mime");
const fs = require("mz/fs");

let readFile = function(ctx, dir){
  return async (ctx, next) => {
    if (ctx.startsWith(dir)) {
      // 
    }else{
      await next();
    }
  }
};